@model PluralSightMVC_App.ViewModels.HomeViewModel

<!DOCTYPE html>

<h2>
    @Model.Title
</h2>
@foreach (var pie in Model.Pies)
{
    <div>
        <h2>@pie.Name</h2>
        <h3>@pie.Price.ToString("c")</h3>
    </div>
}
